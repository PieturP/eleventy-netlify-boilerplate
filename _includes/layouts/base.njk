<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} data-page="{{ page.url | slugify}}" lang="en">
	{% include "components/head.njk" %}
	<body>
		<header>
			{% include "components/nav.njk" %}
		</header>
		<main>
			{{ layoutContent | safe }}
		</main>
		{% include "components/footer.njk" %}
		<div hidden id="snipcart" data-api-key="{{ env.SNIPCART_PUBLIC_KEY }}"></div>
	</body>
</html>
